var gid = require('../lib/core.js').id;

var map = gid(1);


describe('counter', function(){
	it('test obtain a duplicate ID',function(done){
		var map = gid(1)
		//console.log(map);
		//done();
	})
})